test:
  override:
    - echo "No tests!"

deployment:
  production:
    branch: master
    commands:
      - git push git@heroku.com:geochatapi.git `git subtree split --prefix server master`:master --force
